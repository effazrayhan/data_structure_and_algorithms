It contains a Graph Generation Code that takes the number of vertices and edges as input. It then prompts whether the graph is directed or undirected and whether it is weighted or unweighted. Afterward, it collects edge and weight data, displaying the edges in the terminal and saving them in a file named **graph.txt** with the following format:  

**Format:**  
`from_vertex, to_vertex, weight`
